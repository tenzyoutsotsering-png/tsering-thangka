(()=>{const ready=()=>{const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const old=document.getElementById('doorIntro');if(old){old.remove()}const link=document.createElement('link');link.rel='stylesheet';link.href='atelier-motion.css';document.head.appendChild(link);const bar=document.createElement('div');bar.className='scroll-line';document.body.appendChild(bar);const noise=document.createElement('div');noise.className='motion-noise';document.body.appendChild(noise);if(!reduce){const glow=document.createElement('div');glow.className='cursor-glow';document.body.appendChild(glow);addEventListener('pointermove',e=>{glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'});glow.style.opacity='.7'}const hero=document.querySelector('.hero-image');if(hero){hero.classList.add('motion-active');const stage=document.createElement('div');stage.className='motion-stage';const source=hero.querySelector('img');if(source){const frame=document.createElement('div');frame.className='motion-frame';const clone=source.cloneNode(true);frame.appendChild(clone);stage.appendChild(frame)}['p1','p2','p3','p4'].forEach(c=>{const p=document.createElement('i');p.className='pigment '+c;stage.appendChild(p)});const label=document.createElement('div');label.className='motion-label';label.textContent='A painting in progress';stage.appendChild(label);hero.appendChild(stage);if(!reduce){hero.addEventListener('pointermove',e=>{const r=hero.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;stage.style.transform=`rotateX(${-y*4}deg) rotateY(${x*6}deg)`;frame&& (frame.style.transform=`translate3d(${x*10}px,${y*8}px,35px) rotateZ(${x*1.2}deg)`)});hero.addEventListener('pointerleave',()=>{stage.style.transform='';if(frame)frame.style.transform=''});}}document.querySelectorAll('.course,.tile,.fact,.steps div,.section').forEach(el=>el.classList.add('reveal'));if(reduce){document.querySelectorAll('.reveal').forEach(el=>el.classList.add('is-visible'))}else{const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}}),{threshold:.12,rootMargin:'0px 0px -50px'});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));addEventListener('scroll',()=>{const h=document.documentElement.scrollHeight-innerHeight;bar.style.transform=`scaleX(${h>0?scrollY/h:0})`;if(hero){const r=hero.getBoundingClientRect(),p=Math.max(-.5,Math.min(.5,(innerHeight/2-(r.top+r.height/2))/innerHeight));const st=hero.querySelector('.motion-stage');if(st)st.style.translate=`0 ${p*34}px`}}, {passive:true})}};document.readyState==='loading'?document.addEventListener('DOMContentLoaded',ready):ready()})();